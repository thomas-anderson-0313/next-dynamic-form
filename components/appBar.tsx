import {AppBar, Toolbar} from '@material-ui/core';

export default function Bar() {
    return(
        <div>
            <AppBar>
                <Toolbar/>
            </AppBar>
        </div>
    )
}